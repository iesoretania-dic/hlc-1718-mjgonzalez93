{% extends 'layout.html.twig' %}
{% set ruta = app.request.get('_route') %}
{% block title %}{% if ruta == "perfil" %}Modificar perfil{% elseif ruta == "cambiar_password" %}Cambiar contraseña{% endif %}{% endblock %}
{% block content %}
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group">
        <div class="col-sm-2"></div>
        <div class="col-sm-10">
            <button type="submit" name="enviar" class="btn btn-success"><i class="fa fa-check"></i> Guardar cambios
            </button>
            <a href="{{ path('incio') }}" class="btn btn-primary"><i class="fa fa-arrow-left"></i> Volver al inicio</a>
        </div>
    </div>
    {{ form_end(form) }}
{% endblock %}